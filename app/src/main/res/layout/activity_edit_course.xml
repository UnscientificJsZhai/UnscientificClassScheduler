<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/EditCourseActivity_PlusButton"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="24dp"
        android:clickable="true"
        android:contentDescription="@string/activity_EditCourse_AddButtonDescription"
        android:src="@drawable/outline_add_24"
        android:theme="@style/Theme.TimeManager.Widget.FloatingButton"
        android:tint="@color/white"
        app:layout_anchor="@+id/EditCourseActivity_RecyclerView"
        app:layout_anchorGravity="end|bottom"
        app:layout_behavior=".ui.others.CustomFloatingActionButtonBehavior" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/EditCourseActivity_RecyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>